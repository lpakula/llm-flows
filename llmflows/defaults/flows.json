{
  "version": 1,
  "flows": [
    {
      "name": "default",
      "description": "Three-step workflow: understand, implement, validate.",
      "steps": [
        {
          "name": "understand",
          "position": 0,
          "content": "# UNDERSTAND\n\n## PURPOSE\n\nInspect the task and understand the relevant code before making any changes.\n\n## WORKFLOW\n\n1. Read the task description carefully\n2. Identify the files and modules that are relevant\n3. Read through the relevant code to understand the current behavior\n4. Note any conventions, patterns, or constraints in the codebase\n5. Form a clear plan for what needs to change\n\n## RULES\n\n- Do not make any code changes in this step\n- Do not run any commands that modify files\n- Focus only on reading and understanding"
        },
        {
          "name": "implement",
          "position": 1,
          "content": "# IMPLEMENT\n\n## PURPOSE\n\nMake the changes described in the task.\n\n## WORKFLOW\n\n1. Implement the changes based on your understanding from the previous step\n2. Follow the conventions and patterns found in the codebase\n3. Keep changes minimal and focused on the task\n\n## RULES\n\n- No scope expansion beyond what was asked"
        },
        {
          "name": "validate",
          "position": 2,
          "content": "# VALIDATE\n\n## PURPOSE\n\nVerify that your changes work correctly by running tests, builds, or linters.\n\n## WORKFLOW\n\n1. Run the project's test suite, build command, or linter\n2. If anything fails, read the error output carefully\n3. Fix any failures caused by your changes\n4. Re-run until all checks pass\n\n## RULES\n\n- No new features or enhancements\n- Only fix issues related to your changes\n- If the project has no test suite, verify your changes by reviewing them critically"
        }
      ]
    },
    {
      "name": "submit-pr",
      "description": "Create a PR or comment on an existing one.",
      "steps": [
        {
          "name": "submit-pr",
          "position": 0,
          "content": "# SUBMIT PR\n\n## PURPOSE\n\nPush your branch and either create a new PR or comment on an existing one.\n\n## WORKFLOW\n\n1. **Push the branch** to the remote:\n   ```\n   git push origin HEAD\n   ```\n\n2. **Check whether a PR already exists** for the current branch:\n   ```\n   gh pr view --json number,url 2>/dev/null\n   ```\n\n3. **If no PR exists** — create one:\n   - Review changes: `git log main..HEAD --oneline` and `git diff main..HEAD --stat`\n   - Write the PR body using the **PR Template** below\n   - Create the PR:\n     ```\n     cat <<'EOF' > /tmp/pr-body.md\n     <your filled-in PR body>\n     EOF\n     gh pr create --title \"<descriptive title>\" --body-file /tmp/pr-body.md\n     ```\n\n4. **If a PR already exists** — post a comment about the latest changes:\n   - Review what changed since the last commit: `git log main..HEAD --oneline`\n   - Write a concise comment describing what was done in this run\n   - Post the comment:\n     ```\n     cat <<'EOF' > /tmp/pr-comment.md\n     <your comment>\n     EOF\n     gh pr comment --body-file /tmp/pr-comment.md\n     ```\n\n## PR Template (for new PRs)\n\n```markdown\n## Summary\n\nWhat was implemented and why.\n\n## Changes\n\n- `path/to/file.py` — what changed and why\n\n## Test Results\n\nTest commands run and results.\n```\n\n## RULES\n\n- Always push before creating or commenting on a PR\n- Always check for an existing PR first\n- Fill every section with real content -- do not copy templates literally\n- Be specific about what changed in this run",
          "gates": [
            {"command": "test \"$(git rev-parse HEAD)\" = \"$(git rev-parse origin/$(git branch --show-current) 2>/dev/null)\"", "message": "Local commits have not been pushed to the remote. Run git push origin HEAD before advancing."}
          ]
        }
      ]
    }
  ]
}
